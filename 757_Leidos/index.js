document.addEventListener("DOMContentLoaded", () => {
  const lmContent = document.querySelector(".LM_content");
  lmContent.classList.add("animation");

  //example for js
// const topTextImg = document.querySelectorAll(".top-text img");
// topTextImg.forEach(img, index => {
//     const delay = 0.4 * index;
//     img.style.animationDelay = `${delay}s`,`${delay}s`;
//     // img.style.animation = 'appear-items 1s ease-out forwards,move-right-to-left 1s ease-out forwards';
// });
});

